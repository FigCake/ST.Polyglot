/**
 * types.ts — Shared type definitions for the Polyglot extension.
 *
 * Rule: put a type HERE if it is used in two or more modules.
 * Types used only inside a single module live in that module's file instead.
 */
export {};
